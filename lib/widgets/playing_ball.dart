import 'package:flutter/material.dart';

import '../utils/constants.dart';

class PlayingBall extends StatelessWidget {
  const PlayingBall({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(firstBall);
  }
}
